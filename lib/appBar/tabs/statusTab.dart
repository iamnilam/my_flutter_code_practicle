import 'package:flutter/material.dart';

class StatusTabsScreen extends StatelessWidget {
  const StatusTabsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Status"),
      ),
    );
  }
}

